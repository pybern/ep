#!/bin/bash
set -e

#Command variable
ANSIBL_VAULT_ACTION=$1
ANSIBL_VAULT_INPUT_FILE=$2

ansible-vault "$ANSIBL_VAULT_ACTION" "$ANSIBL_VAULT_INPUT_FILE" --vault-password-file="$ANSIBLE_VAULT_SECRET_PATH"
